const Transaction = require("./Transaction")

describe('Transaction', () => {
    it("creates a new transaction that has type:deposit and amount:1000")
    const transaction = new Transaction("deposit", 1000)
    // expect Transaction.
})